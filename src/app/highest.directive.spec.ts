import { HighestDirective } from './highest.directive';

describe('HighestDirective', () => {
  it('should create an instance', () => {
    const directive = new HighestDirective();
    expect(directive).toBeTruthy();
  });
});
